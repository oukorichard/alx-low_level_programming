#include <stdio.h>
int rich(int i, int j);// function type decleration//

int main ()
{
  int a, b, c;

  for(a = 97; a <= 123; a++)
  {
  putchar (a);
  }
  return 0;

  int rich(int i, int j);
  {
    if (b<=10)
    
    return putchar (b)
  }
}
