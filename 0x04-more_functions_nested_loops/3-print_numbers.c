#include "main.h"

/**
 * print_numbers - prints numbers between 0 to 9.
 * Return: no return.
 */
void print_numbers(void)
{
	char h;

	for (h = 0; ch < 9; h++)
	{
		_putchar(h);
	}
	_putchar('\n');
}
